services:
  - type: web
    name: renegociacao-boletos-ixc
    runtime: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: IXC_BASE_URL
        sync: false
      - key: IXC_TOKEN
        sync: false
